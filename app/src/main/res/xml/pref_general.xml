<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:key="@string/pref_sort_key"
        android:title="@string/pref_sort_label"
        android:dialogTitle="@string/pref_sort_dialog_title"
        android:defaultValue="@string/pref_posters_sort_popularity"
        android:entries="@array/pref_posters_sort_titles"
        android:entryValues="@array/pref_posters_sort_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
